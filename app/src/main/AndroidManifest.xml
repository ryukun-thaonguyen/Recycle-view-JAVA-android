<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.recyclerview_n_sqlite.example.sqlitetutorial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.recyclerview_n_sqlite.example.sqlitetutorial.UpdateActivity"
            android:parentActivityName="com.recyclerview_n_sqlite.example.sqlitetutorial.MainActivity"/>
        <activity
            android:name="com.recyclerview_n_sqlite.example.sqlitetutorial.AddActivity"
            android:label="Add Book"
            android:parentActivityName="com.recyclerview_n_sqlite.example.sqlitetutorial.MainActivity" />
        <activity
            android:name="com.recyclerview_n_sqlite.example.sqlitetutorial.MainActivity"
            android:label="Book Library">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>